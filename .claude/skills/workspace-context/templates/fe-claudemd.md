# {APP_NAME}

> Auto-generated by `workspace-context` skill. Regenerate with `/workspace-context --workspace {app-id}`.

## Overview

{One-paragraph description of what this app does and its role in the platform.}

## Tech Stack

| Layer | Technology |
|---|---|
| Language | TypeScript {version} |
| Framework | React {version} |
| Bundler | Vite {version} |
| Routing | React Router v{version} |
| Server State | TanStack React Query v{version} |
| Client State | Zustand v{version} |
| Linter/Formatter | Biome |
| Tests | Vitest + Testing Library |
| API Mocking | MSW v{version} |

{Include additional deps if present, e.g. Stripe}

## Routes

> Source: `docs/routes.md` (CI-generated, read-only)

| Path | Component | Auth Required |
|---|---|---|
| `{path}` | {component} | {yes/no} |

## Consumed Backend Endpoints

> Source: `docs/consumed-endpoints.md` (CI-generated, read-only)

### {service-name}

| Method | Path | Client File |
|---|---|---|
| {METHOD} | `{path}` | `{source-file}` |

## State Management

| Store | File | Purpose |
|---|---|---|
| `{storeName}` | `src/stores/{file}.ts` | {what it manages} |

## Environment Variables

| Variable | Default | Used In |
|---|---|---|
| `{VITE_VAR_NAME}` | {default or —} | `{source-file}` |

## Directory Structure

```
src/
  api/             # API client modules (fetch wrappers)
  components/      # Shared UI components
  features/        # Feature modules (co-located components + hooks)
  hooks/           # Custom React hooks
  pages/           # Page-level components (one per route)
  stores/          # Zustand stores
  providers/       # Context providers
  mocks/           # MSW handlers (dev + test)
  types/           # TypeScript type definitions
  App.tsx          # Route definitions
  main.tsx         # Entry point, provider stack
tests/
  integration/     # Integration tests (TS-XXX-XXX describe blocks)
  setup.ts         # Test setup (MSW server, mocks)
docs/
  routes.md              # CI-generated route manifest
  consumed-endpoints.md  # CI-generated endpoint dependencies
scripts/
  generate-specs.mjs     # Spec generation script (CI runs this)
```

## Dev Commands

| Command | What it Does |
|---|---|
| `npm install` | Install dependencies |
| `npm run dev` | Start Vite dev server |
| `npm run build` | Type-check + production build |
| `npm test` | Run all tests (Vitest) |
| `npm run typecheck` | Type check only (`tsc --noEmit`) |
| `npm run lint` | Lint with Biome |
| `node scripts/generate-specs.mjs` | Regenerate route/endpoint specs |

## Key Patterns

- **Provider stack**: `StrictMode` → `QueryClientProvider` → `BrowserRouter` (check `main.tsx` and `App.tsx`)
- **API clients**: Centralized fetch wrappers with `X-Tenant-ID` header injection
- **Tenant isolation**: `X-Tenant-ID` header sent on every API request
- **Auth guard**: `<RequireAuth>` layout route redirects unauthenticated users (if applicable)
- **MSW mocking**: `src/mocks/handlers.ts` for dev, `src/mocks/server.ts` for tests
- **Test naming**: `describe("TS-XXX-XXX", ...)` blocks map to TS scenarios
