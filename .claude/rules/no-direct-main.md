# Rule: Never Commit to Main

Do not commit directly to `main` in any repo — spec-hub or workspace.
Always work on a feature branch. If unsure what branch you're on, check with `git branch` before committing.

See `.claude/rules/branching-strategy.md` for naming conventions and automatic branch creation rules.
